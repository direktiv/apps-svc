functions:
- id: pq
  type: subflow
  workflow: "/pq"
states:
- id: a
  type: action
  action:
    function: pq
    input:
      queries:
        - 'CREATE TABLE IF NOT EXISTS apps (uri TEXT, tag TEXT, created_at TIMESTAMP DEFAULT now(), PRIMARY KEY (uri, tag))'

